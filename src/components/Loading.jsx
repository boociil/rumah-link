

const Loading = ({w,h}) => {
    return (
         
        // <!-- From Uiverse.io by Fresnel11 --> 
        <div
        className={`w-${w} h-${h} border-4 dark:border-t-[#4b5563] border-t-sky-500 border-gray-100  rounded-full animate-spin`}
        ></div>

    )
}

export default Loading;